# 定制规则集

> 版本：1.0.0 | 平台：HarmonyOS | 更新日期：2026-05-15
> 适用范围：项目特定的量化推荐值、特定设备形态、特定应用场景。按需启用，并非所有项目都必须遵守

## 约束列表

```yaml
constraints:

# =====================================================================
# 一、量化推荐值
# =====================================================================
# 以下规则提供各组件属性在不同断点下的具体推荐值。
# 不同项目/设计模板可根据实际需求调整这些值。

# ---------------------------------------------------------------------
# List 组件
# ---------------------------------------------------------------------

- id: CST-LIST-01
  name: List lanes 推荐值
  priority: P1
  kit:
    - 'ArkUI: List'
  rules:
  - target: '**/*.ets'
    llm: "检查 List 组件 lanes 属性的推荐值：sm:1, md:2, lg:3, xl:3。实际值与推荐值不同即判定为不满足"

- id: CST-LIST-02
  name: List space 推荐值
  priority: P1
  kit:
    - 'ArkUI: List'
  rules:
  - target: '**/*.ets'
    llm: "检查 List 组件 space 属性的推荐值：sm:8, md:12, lg:16。使用 WidthBreakpointType(8, 12, 16, 16).getValue(widthBp) 为推荐写法。实际值与推荐值不同即判定为不满足"

# ---------------------------------------------------------------------
# WaterFlow 组件
# ---------------------------------------------------------------------

- id: CST-WF-01
  name: WaterFlow columnsTemplate 推荐值
  priority: P1
  kit:
    - 'ArkUI: WaterFlow'
  rules:
  - target: '**/*.ets'
    llm: "检查 WaterFlow 组件 columnsTemplate 的推荐值：sm:repeat(2,1fr), md:repeat(3,1fr), lg:repeat(4,1fr)。实际值与推荐值不同即判定为不满足"

- id: CST-WF-02
  name: WaterFlow gap 推荐值
  priority: P1
  kit:
    - 'ArkUI: WaterFlow'
  rules:
  - target: '**/*.ets'
    llm: "检查 WaterFlow 组件是否设置了 columnsGap 和 rowsGap，推荐值 12vp。缺失 gap 属性或值不为 12vp 即判定为不满足"

# ---------------------------------------------------------------------
# Swiper 组件
# ---------------------------------------------------------------------

- id: CST-SWP-01
  name: Swiper displayCount 推荐值
  priority: P1
  kit:
    - 'ArkUI: Swiper'
  rules:
  - target: '**/*.ets'
    llm: "检查 Swiper 组件 displayCount 的推荐值：sm:1, md:2, lg:3。使用 WidthBreakpointType(1, 2, 3, 3).getValue(widthBp) 为推荐写法。实际值与推荐值不同即判定为不满足"

- id: CST-SWP-02
  name: Swiper prevMargin/nextMargin 推荐值
  priority: P1
  kit:
    - 'ArkUI: Swiper'
  rules:
  - target: '**/*.ets'
    llm: "检查 Swiper 组件在 displayCount >= 2 时的 prevMargin 和 nextMargin 推荐值：md:12vp, lg:64vp。实际值与推荐值不同即判定为不满足"

# ---------------------------------------------------------------------
# Grid 组件
# ---------------------------------------------------------------------

- id: CST-GRID-01
  name: Grid columnsTemplate 推荐值
  priority: P1
  kit:
    - 'ArkUI: Grid'
  rules:
  - target: '**/*.ets'
    llm: "检查 Grid 组件 columnsTemplate 的推荐值：sm:repeat(2,1fr), md:repeat(3,1fr), lg:repeat(4,1fr), xl:repeat(12,1fr)。实际值与推荐值不同即判定为不满足"

- id: CST-GRID-02
  name: Grid gap 推荐值
  priority: P1
  kit:
    - 'ArkUI: Grid'
  rules:
  - target: '**/*.ets'
    llm: "检查 Grid 组件是否设置了 columnsGap 和 rowsGap，推荐值 12vp。缺失 gap 属性或值不为 12vp 即判定为不满足"

# ---------------------------------------------------------------------
# SideBarContainer 组件
# ---------------------------------------------------------------------

- id: CST-SB-01
  name: SideBarContainer sideBarWidth 推荐值
  priority: P1
  kit:
    - 'ArkUI: SideBarContainer'
  rules:
  - target: '**/*.ets'
    llm: "检查 SideBarContainer 组件 sideBarWidth 的推荐值：sm:'80%'（浮层时占大部分宽度）, md:'50%', lg:'40%', xl:固定值如 240vp（xl 断点应设置 sideBarWidth、minSideBarWidth、maxSideBarWidth 为相同固定值确保宽度稳定）。实际值与推荐值不同即判定为不满足"

# ---------------------------------------------------------------------
# Navigation 组件
# ---------------------------------------------------------------------

- id: CST-NAV-01
  name: Navigation navBarWidth 推荐值
  priority: P1
  kit:
    - 'ArkUI: Navigation'
  rules:
  - target: '**/*.ets'
    llm: "检查 Navigation 组件在 Split 模式下 navBarWidth 的推荐值：md:'50%', lg:'30%'。实际值与推荐值不同即判定为不满足"

# ---------------------------------------------------------------------
# Tabs 组件
# ---------------------------------------------------------------------

- id: CST-TAB-01
  name: Tabs lg 侧边导航推荐值
  priority: P1
  kit:
    - 'ArkUI: Tabs'
  rules:
  - target: '**/*.ets'
    llm: "检查 Tabs 组件在 lg 断点侧边导航模式下的推荐值：barWidth:96vp, barHeight:'100%'。实际值与推荐值不同即判定为不满足"

# ---------------------------------------------------------------------
# GridRow / GridCol
# ---------------------------------------------------------------------

- id: CST-GR-01
  name: GridRow columns 推荐值
  priority: P1
  kit:
    - 'ArkUI: GridRow'
  rules:
  - target: '**/*.ets'
    llm: "检查 GridRow 组件 columns 的推荐值：{sm:4, md:8, lg:12, xl:12}。实际值与推荐值不同即判定为不满足"

- id: CST-GR-02
  name: GridRow gutter 推荐值
  priority: P1
  kit:
    - 'ArkUI: GridRow'
  rules:
  - target: '**/*.ets'
    llm: "检查 GridRow 组件 gutter 的推荐值：sm:8/12, md:16, lg:24。实际值与推荐值不同即判定为不满足"

- id: CST-GR-03
  name: GridCol span 挪移和缩进布局推荐值
  priority: P1
  kit:
    - 'ArkUI: GridCol'
  rules:
  - target: '**/*.ets'
    llm: "检查 GridCol 组件 span 的推荐值：挪移布局（图文组合）sm 断点插图和文字 span 均占满全部列数实现上下排列，lg 断点插图 span 为 4、文字 span 为 8 实现左右排列；缩进布局（单列内容）sm:4/md:6/lg:8 配合 offset 实现居中缩进。实际值与推荐值不同即判定为不满足"

- id: CST-GR-04
  name: GridCol offset 缩进布局推荐值
  priority: P1
  kit:
    - 'ArkUI: GridCol'
  rules:
  - target: '**/*.ets'
    llm: "检查 GridCol 组件 offset 的推荐值：sm offset:0, md offset:1（两侧各留 1 列）, lg offset:2（两侧各留 2 列）。实际值与推荐值不同即判定为不满足"

# =====================================================================
# 二、特定布局模式
# =====================================================================

# ---------------------------------------------------------------------
# 三分栏
# ---------------------------------------------------------------------

- id: CST-3COL-01
  name: 三分栏组合必须同时控制 SideBarContainer 和 Navigation 属性
  priority: P0
  kit:
    - 'ArkUI: SideBarContainer'
    - 'ArkUI: Navigation'
  rules:
  - target: '**/*.ets'
    llm: "检查三分栏布局中 SideBarContainer 和 Navigation 组件是否同时根据断点设置：sm 断点 SideBarContainer.showSideBar 为 false + SideBarContainerType 为 Overlay + Navigation.mode 为 Stack（单栏无侧边栏）；md 断点 SideBarContainer.showSideBar 为 false + Navigation.mode 为 Split + navBarWidth 为 '50%'（双栏无侧边栏）；lg 断点 SideBarContainer.showSideBar 为 true + SideBarContainerType 为 Embed + sideBarWidth 为 '20%' + Navigation.mode 为 Split + navBarWidth 为 '30%'（三栏并排）；xl 断点同 lg 实现三分栏。仅控制其中一个组件的属性即判定失败"

- id: CST-3COL-02
  name: 三分栏 lg 断点 SideBarContainer 必须使用 Embed 模式
  priority: P0
  kit:
    - 'ArkUI: SideBarContainer'
  rules:
  - target: '**/*.ets'
    llm: "检查三分栏布局在 lg 断点下 SideBarContainer 的类型是否为 SideBarContainerType.Embed（嵌入模式），确保三栏并排显示。lg 断点仍使用 Overlay（浮层模式）导致侧边栏遮挡内容区即判定失败"

# ---------------------------------------------------------------------
# xl / PC 分级导航
# ---------------------------------------------------------------------

- id: CST-XL-01
  name: xl 断点或 PC 设备必须使用 SideBarContainer 替代 Tabs 实现分级导航
  priority: P0
  kit:
    - 'ArkUI: SideBarContainer'
    - 'ArkUI: Tabs'
  rules:
  - target: '**/*.ets'
    llm: "当横向断点为 xl 或设备为 2in1 时，检查是否使用 SideBarContainer(SideBarContainerType.Embed) 实现分级导航（包含一级和二级导航目录），替代 Tabs 组件。xl 断点仍仅使用 Tabs 无法展示分级导航结构即判定失败"

# =====================================================================
# 三、折叠屏纵向断点
# =====================================================================

- id: CST-FOLD-01
  name: 折叠屏和特殊窗口场景必须同时使用纵向断点
  priority: P0
  kit:
    - 'ArkUI: HeightBreakpoint'
  rules:
  - target: '**/*.ets'
    llm: "当适配目标含 foldable 或存在类方形窗口场景时，检查断点系统是否同时提供纵向断点（HeightBreakpoint: HEIGHT_SM/MD/LG，按窗口高宽比划分）。仅使用横向断点无法区分 Pura X 外屏（sm+md）和直板机竖屏（sm+lg）即判定失败"

- id: CST-FOLD-02
  name: 横向窗口判断必须使用纵向断点为 sm 的条件
  priority: P0
  kit:
    - 'ArkUI: HeightBreakpoint'
  rules:
  - target: '**/*.ets'
    llm: "检查需要区分横向窗口的逻辑，判断条件必须使用 currentHeightBreakpoint === HeightBreakpoint.HEIGHT_SM（高宽比<0.8），而非 deviceType 或屏幕方向 API。类方形窗口判断必须使用 HEIGHT_MD（高宽比在 [0.8,1.2)）。使用非断点方式判断窗口形态即判定失败"

- id: CST-FOLD-03
  name: 类方屏旋转方案必须通过纵向断点 md 触发
  priority: P0
  kit:
    - 'ArkUI: HeightBreakpoint'
  rules:
  - target: '**/*.ets'
    llm: "检查类方屏（如 Mate X5 展开态）窗口旋转逻辑，设置 window.setPreferredOrientation(AUTO_ROTATION_RESTRICTED) 的条件必须包含纵向断点为 md 的判断（HeightBreakpoint.HEIGHT_MD），而非使用 isFoldable() + getFoldStatus() 组合。使用设备类型或折叠状态判断旋转策略即判定失败"

# =====================================================================
# 四、小方形屏（sm + md）：Pura X 外屏、折叠屏外屏等
# =====================================================================

- id: CST-SQR-01
  name: 小方形屏页面必须使用 Scroll 包裹保证内容可滑动
  priority: P0
  kit:
    - 'ArkUI: Scroll'
  rules:
  - target: '**/*.ets'
    llm: "当横向断点为 sm 且纵向断点为 md 时，检查页面根容器是否使用 Scroll 组件包裹。小方形屏内容高度减半，无 Scroll 会导致内容截断不可见即判定失败"

- id: CST-SQR-02
  name: 小方形屏自定义弹窗必须使用 constraintSize 限制最大高度
  priority: P0
  kit:
    - 'ArkUI: Scroll'
  rules:
  - target: '**/*.ets'
    llm: "当横向断点为 sm 且纵向断点为 md 时，检查自定义弹窗的内容区是否使用 Scroll 包裹并设置 constraintSize({ maxHeight: '90%' })。弹窗内容未限制最大高度导致超出小方形屏范围即判定失败"

- id: CST-SQR-03
  name: 小方形屏短视频页面侧边控件应支持滑动
  priority: P1
  kit:
    - 'ArkUI: Scroll'
    - 'ArkUI: Blank'
  rules:
  - target: '**/*.ets'
    llm: "当横向断点为 sm 且纵向断点为 md 时，检查短视频类页面的侧边控件区域是否使用 Scroll 组件包裹，并配合 Blank + displayPriority 控制上下留白的显隐。侧边控件区无 Scroll 导致部分控件被截断即判定为不满足"

- id: CST-SQR-04
  name: 小方形屏应支持沉浸式浏览
  priority: P1
  kit:
    - 'ArkUI: Scroll'
  rules:
  - target: '**/*.ets'
    llm: "当横向断点为 sm 且纵向断点为 md 时，检查内容列表页面是否实现了上滑隐藏标题栏/页签栏、下滑恢复的沉浸式浏览效果（通过监听滚动偏移量动态调整高度和透明度）。未实现沉浸式浏览即判定为不满足"

# =====================================================================
# 五、圆形屏 / wearable（xs + sm）：智能手表
# =====================================================================

- id: CST-WCH-01
  name: 圆形屏页面根容器必须设置 borderRadius 为 50%
  priority: P0
  kit:
    - 'ArkUI: borderRadius'
  rules:
  - target: '**/*.ets'
    llm: "当适配目标含 wearable 时，检查手表页面的最外层容器是否设置了 .borderRadius('50%')，确保内容适配圆形表盘。缺失 borderRadius 或值不为 50% 即判定失败"

- id: CST-WCH-02
  name: 圆形屏列表必须使用 ArcList 而非 List
  priority: P0
  kit:
    - 'ArkUI: ArcList'
  rules:
  - target: '**/*.ets'
    llm: "当适配目标含 wearable 时，检查手表模块中所有列表组件必须使用 ArcList + ArcListItem，不得使用普通 List/ListItem。使用普通 List 导致列表项两端被圆形屏幕裁切即判定失败"

- id: CST-WCH-03
  name: 圆形屏页面切换必须使用 ArcSwiper
  priority: P0
  kit:
    - 'ArkUI: ArcSwiper'
  rules:
  - target: '**/*.ets'
    llm: "当适配目标含 wearable 时，检查手表页面间滑动切换场景是否使用 ArcSwiper 组件而非普通 Swiper。使用普通 Swiper 在圆形屏上无法正确显示轮播效果即判定失败"

- id: CST-WCH-04
  name: 圆形屏内容布局必须避免边缘信息丢失
  priority: P0
  kit:
    - 'ArkUI: ArcList'
  rules:
  - target: '**/*.ets'
    llm: "当适配目标含 wearable 时，检查 ArcListItem 内的内容根节点宽度设置，内容宽度应控制在约 90%（而非 100%），确保文字和图标不被圆形边缘裁切。内容宽度为 100% 导致两端被裁切即判定失败"

- id: CST-WCH-05
  name: 圆形屏横向切屏必须使用 ArcSwiper 分页展示
  priority: P1
  kit:
    - 'ArkUI: ArcSwiper'
  rules:
  - target: '**/*.ets'
    llm: "当适配目标含 wearable 时，检查内容量超过单屏的页面是否使用 ArcSwiper 实现横向切屏（左右滑动切换不同信息页），而非将所有内容垂直堆叠。垂直堆叠过多内容导致可读性差即判定为不满足"

# =====================================================================
# 六、应用场景
# =====================================================================

# ---------------------------------------------------------------------
# 聊天场景
# ---------------------------------------------------------------------

- id: CST-CHAT-01
  name: 聊天场景双栏时内容扩展页必须切回 Stack 单栏
  priority: P0
  kit:
    - 'ArkUI: Navigation'
  rules:
  - target: '**/*.ets'
    llm: "检查社交类应用的聊天页面：双栏模式下（md/lg 断点）点击右侧内容区的扩展链接（如商品链接）时，Navigation.mode 必须从 Split 切回 Stack 全屏展示扩展内容。双栏模式下扩展内容仍在分栏右侧展示而非全屏即判定失败"

- id: CST-CHAT-02
  name: 聊天场景返回时应恢复 Split 双栏模式
  priority: P1
  kit:
    - 'ArkUI: Navigation'
  rules:
  - target: '**/*.ets'
    llm: "检查聊天页面从扩展内容返回时，Navigation.mode 是否恢复为 Split 双栏。返回后仍保持 Stack 单栏即判定为不满足"

# ---------------------------------------------------------------------
# 邮箱场景
# ---------------------------------------------------------------------

- id: CST-MAIL-01
  name: 邮箱场景 lg 断点应默认显示三栏
  priority: P1
  kit:
    - 'ArkUI: SideBarContainer'
    - 'ArkUI: Navigation'
  rules:
  - target: '**/*.ets'
    llm: "检查邮箱类应用在 lg 断点下是否默认显示三栏：左侧账户信息（SideBarContainer 侧边栏）、中间收件箱（Navigation 导航栏）、右侧邮件详情（Navigation 内容区）。lg 断点默认隐藏侧边栏需手动打开即判定为不满足"

# ---------------------------------------------------------------------
# 日历场景
# ---------------------------------------------------------------------

- id: CST-CAL-01
  name: 日历场景单栏时应优先展示导航区而非内容区
  priority: P1
  kit:
    - 'ArkUI: Navigation'
  rules:
  - target: '**/*.ets'
    llm: "检查日历日程类应用在 sm 单栏模式下，Navigation 是否优先展示日历（导航栏内容）而非日程详情（内容区）。实现方式：在 onNavigationModeChange 中检测到 Stack 模式时调用 pathStack.clear() 清空内容区路由。单栏时默认显示空白详情页而非日历即判定为不满足"
```
